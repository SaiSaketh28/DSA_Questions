// lower bound gives the value to be found  in container or array, if the element is which is to be found in container is not present in it then it returns the a liiitle higher value number than it in the array if there is no element higher than it then it returns pointer of (nth) location in array or container
// upper bound always returns a higher value
#include <bits/stdc++.h>
using namespace std;
int main() {
#ifndef ONLINE_JUDGE
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
    #endif


return 0;
}